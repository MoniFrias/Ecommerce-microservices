server:
  port: 8090
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/MoniFrias/Ecommerce-microservices.git
          search-paths: config-data
